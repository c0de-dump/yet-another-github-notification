'use strict'

/** @type {import("prettier").Options} */
const config = {
    printWidth: 160,
    tabWidth: 4,
    singleQuote: true,
    useTabs: false,
    semi: false,
    trailingComma: 'es5',
}

module.exports = config
